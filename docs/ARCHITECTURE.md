# Как работает (под капотом)

## Пять инструментов MCP

| Инструмент                      | Что делает                                                                                                           |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `rlm_projects(action, ...)`     | Управление реестром проектов: `list`, `add`, `remove`, `rename`, `update`. Связка имя → путь к исходникам. Все мутирующие операции требуют `password`; смена пароля — через `clear_password` + повторный `update(password=...)` |
| `rlm_index(action, ...)`        | Управление SQLite-индексом методов: `build` (полная сборка), `update` (инкрементальное обновление), `info` (статистика), `drop` (удаление). build/update выполняются в фоне (`{"started": true}`), статус через info. build/update/drop требуют зарегистрированный проект с паролем (CLI `rlm-bsl-index` работает без пароля). С v1.21.0 инкрементальный `update` держит граф вызовов консистентным со свежей сборкой (пере-резолв привязок `callee_key` при изменении общих модулей), без пересборки индекса. С v1.32.0 **новое** построение индекса на формате, отличном от cf/edt, через MCP отклоняется до постановки фоновой job — подтвердить заведомо неполную сборку может только человек в терминале (`rlm-bsl-index index build <path> --allow-unsupported-format`) |
| `rlm_start(path, query)`        | Открывает сессию на каталоге исходников. Определяет формат (CF/EDT), строит индекс BSL-файлов, возвращает session_id |
| `rlm_execute(session_id, code)` | Выполняет Python-код в песочнице. Данные остаются на сервере, в контекст попадает только stdout                      |
| `rlm_end(session_id)`           | Закрывает сессию, освобождает ресурсы                                                                                |

## Что происходит при `rlm_start`

1. **Определение формата** — сканирует структуру каталога и определяет: CF (Конфигуратор, каталоги `Ext/`), EDT (1C:EDT, файлы `.mdo`), или смешанный
2. **Классификация поддержки** (v1.32.0) — отдельно от формата решается, наш ли это формат вообще. Признак — **валидный дескриптор** CF (`Configuration.xml` с корнем `{MDClasses}MetaDataObject` и первым дочерним `Configuration`) или EDT (`Configuration/Configuration.mdo` с корнем `{mdclass}Configuration`); дескриптор ищется в корне, прямом подкаталоге или через один каталог-обёртку (`cfe/ExtA/…`), читается **префиксно** (два первых start-события `iterparse`) и не парсится целиком. Одного имени файла недостаточно: чужой `docs/Configuration.xml` гейт не обходит. Результат уезжает в ответ полем `source_support`:
   - `supported` — поведение сессии без изменений;
   - `foreign_with_bsl` — чужая раскладка, но `.bsl` есть: сессия работает, BSL-хелперы остаются, первым идёт предупреждение (и в `warnings[0]`, и в начале `strategy`), результат объявляется best-effort;
   - `foreign_no_bsl` — чужая раскладка без единого `.bsl`: **generic-режим**, BSL-хелперы в песочницу не загружаются вовсе, стратегия своя (`build_generic_strategy`) и перечисляет только базовые файловые хелперы. Нумерация строк `read_file`/`read_files`/`grep_read` сохраняется.

   Классификация выполняется на **каждом** `rlm_start`, включая index fast path (индекс мог быть собран до появления гейта), поэтому она обязана быть дешёвой: листинги каталогов только уровней 0–1, кандидаты уровня 2 — точечные `open()`. На боевых cf/edt — единицы миллисекунд.
3. **Построение индекса** — находит все `.bsl` файлы, для каждого определяет: категорию (`Documents`, `Catalogs`, `CommonModules`...), имя объекта, тип модуля (`ObjectModule`, `ManagerModule`, `FormModule`...)
4. **Дисковый кэш** — индекс сохраняется на диске в `%USERPROFILE%\.cache\rlm-tools-bsl\<хэш_пути>\file_index.json` (Windows) или `~/.cache/rlm-tools-bsl/<хэш_пути>/file_index.json` (Linux/macOS). При повторном запуске на том же каталоге (если количество .bsl файлов не изменилось) — индекс загружается мгновенно
5. **Детектирование расширений** — XML-парсинг `Configuration.xml` / `Configuration.mdo` для определения, является ли каталог основной конфигурацией или расширением. Автоматическое сканирование соседних каталогов (1-2 уровня вверх) на наличие расширений или основной конфигурации. Запуск на standalone-расширении поддерживается (is_extension=True). Запуск на расширении внутри контейнера (напр. `src/cfe/Ext1/`) — не поддерживаемый сценарий; рекомендуется запускать на основной конфигурации, расширения из соседних каталогов (включая контейнеры `src/cfe/` с несколькими расширениями) обнаруживаются автоматически
6. **Хелперы** — в песочницу добавляются BSL-специфичные функции для навигации по коду (в generic-режиме — только базовые файловые)
7. **Сессионные кэши** — в рамках одной сессии кэшируются: содержимое файлов (LRU, до 500 файлов), результаты `extract_procedures()` (по пути файла), результаты grep (LRU, до 100 запросов), списки файлов-кандидатов для `find_callers_context()`. Благодаря этому повторные вызовы при построении графа вызовов работают мгновенно — файлы не перечитываются и не парсятся заново

## Таймауты и безопасность

Каждый вызов `rlm_execute` ограничен таймаутом (по умолчанию 45 секунд, настраивается через `execution_timeout_seconds` в `rlm_start`). Если код не успевает завершиться — выполнение прерывается, но всё, что успело попасть в `stdout`, сохраняется и возвращается агенту. Это позволяет получить частичные результаты даже при таймауте.

На Unix используется сигнальный механизм (`SIGALRM`), на Windows — `threading.Timer` с инъекцией исключения через `PyThreadState_SetAsyncExc`.

```text
MCP server (родительский процесс)
  ├─ Session A → Sandbox worker A → Sandbox A + namespace A + IndexReader A
  ├─ Session B → Sandbox worker B → Sandbox B + namespace B + IndexReader B
  └─ Session C → Sandbox worker C → Sandbox C + namespace C + IndexReader C
```

Worker живёт от успешного `rlm_start` до `rlm_end`/TTL-эвикции/аварии/остановки сервера — переменные и кэши сессии сохраняются между `rlm_execute` внутри worker. Это даёт:

- **stdout физически разделён процессами** — параллельные `rlm_execute` разных сессий не могут смешать вывод (в v1.28 такая гонка была воспроизведена: `redirect_stdout` подменял глобальный `sys.stdout` общего процесса);
- **hard wall-clock kill**: авторитетный deadline (по умолчанию 45с, `execution_timeout_seconds` в `rlm_start`, ≤300с) контролирует родитель. По истечении дерево процессов worker уничтожается целиком — Windows Job Object (`KILL_ON_JOB_CLOSE`), POSIX process group (`setsid`+`killpg`) — независимо от того, что делает код: перехват `BaseException`, зависший C-extension, catastrophic regex, блокирующий I/O, запущенные дочерние процессы;
- **частичный stdout**: вывод пишется worker-ом напрямую в shared-буфер с публикацией по целым UTF-8 символам, поэтому всё напечатанное до kill возвращается агенту с маркером `... [execution terminated after timeout; partial output]`;
- **потеря состояния — явная**: после kill все переменные/кэши worker потеряны; ответ несёт machine-readable `sandbox_state={status:"terminated", reason:..., state_lost:true, restart:"on_next_execute"}`, следующий `rlm_execute` лениво поднимает чистое поколение (`sandbox_state.status="restarted"` в его первом ответе). Счётчики execute/LLM-вызовов сессии живут в родителе и сохраняются;
- **crash/OOM worker не роняет MCP-сервер** — сессия получает controlled error и lazy restart;
- **лимиты на worker**: память (`RLM_SANDBOX_MEMORY_MB`, Job Object / `RLIMIT_AS`), размер кода и IPC-фреймов; вывод ограничивается в момент `write`, а не после выполнения.

IPC parent↔worker — только UTF-8 JSON-фреймы с protocol version/allowlist типов/request id/generation и лимитом размера (`send_bytes`/`recv_bytes(maxlength)`); pickle для runtime-сообщений от запущенного worker не используется. Raw fd 0/1/2 worker отвязаны от родителя до выполнения любых команд — escaped-код не может испортить MCP stdio framing.

Отдельный случай — окружения, где режим `process` недоступен **физически**: пролог запуска worker создает именованный канал, разделяемую память и семафоры, и MCP-клиент с файловой песочницей может запретить дочернему процессу любой из них — тогда `rlm_start` отказывает с `PermissionError` еще до создания сессии, и `inline` там не «аварийный fallback», а единственный рабочий режим (диагностика — в [INSTALL.md](INSTALL.md)).

Режим `inline` (`RLM_SANDBOX_MODE=inline`) сохраняет прежнее выполнение в основном процессе — только для диагностики и аварийного восстановления. stdout-гонка закрыта и там (глобальная сериализация всех inline-execute), но hard-kill НЕ гарантируется: используется прежний fallback-таймаут — `SIGALRM` на Unix, `threading.Timer` + `PyThreadState_SetAsyncExc` на Windows/не-main-thread, — который код может пережить. Автоматического fallback `process` → `inline` нет; невалидный `RLM_SANDBOX_MODE` — ошибка старта сервера.

### Параллельность и бюджет памяти

Каждая сессия — это отдельный worker-процесс, поэтому сессии изолированы по namespace и памяти, а параллельные `rlm_execute` разных сессий физически идут в разных процессах. Модель памяти при этом устроена так, что «поднять лимиты» почти никогда не нужно, а интуиция про них легко даёт осечку:

- **`RLM_SANDBOX_MEMORY_MB` — это ПОТОЛОК, а не резервирование и не floor.** Worker (Job Object process memory limit на Windows / `RLIMIT_AS` на Linux) занимает ровно столько, сколько реально нужно коду сессии: namespace + собственный `IndexReader` + прочитанные тела файлов. При потолке 1024 и 512 фактический RSS при обычной нагрузке одинаков. **Понижение потолка нормальную память НЕ уменьшает** — оно лишь сдвигает порог, на котором убивают ОДНУ разросшуюся сессию: утечка или огромная аллокация ловит `MemoryError`/kill раньше, чем утащит хост в swap/OOM. Это тугой предохранитель против одного патологического сеанса, а не рычаг суммарного расхода.
- **Суммарный расход контролирует число воркеров, а не потолок.** Worst-case потолок всего сервера ≈ `RLM_MAX_SESSIONS × RLM_SANDBOX_MEMORY_MB + память parent` — но это ВЕРХНЯЯ граница, а не факт: реальный RSS почти всегда кратно ниже. Чтобы ограничить общий расход на слабом хосте, снижают **`RLM_MAX_SESSIONS`** (меньше одновременных воркеров = меньше параллельных RSS), а не память на воркер.
- **Порядок величин.** Даже полный разбор тяжёлого документа ERP (десятки модулей, сотни методов) держит в namespace worker единицы МБ сырых тел + разобранные структуры; `IndexReader` добавляет десятки–сотни МБ рабочего набора SQLite (страницы кешируются по требованию, а не грузятся целиком). Дефолтный потолок 1 ГБ/воркер оставляет большой запас — его имеет смысл при желании ПОНИЖАТЬ (тугой предохранитель), а не повышать.
- **Что реально едет по IPC.** `execute_result`-фрейм несёт только `error` + ИМЕНА переменных + `helper_calls` + hints — НЕ stdout (он публикуется в отдельный shared-буфер) и НЕ ЗНАЧЕНИЯ переменных. Крупный результат хелпера остаётся в namespace worker и IPC не пересекает; `RLM_SANDBOX_IPC_MAX_BYTES` (4 МиБ) ограничивает лишь размер одного сообщения и на практике недостижим — кроме патологии (тысячи `helper_calls` в одном execute или гигантский traceback, который к тому же авто-обрезается под фрейм).

### Параллельные сессии на одном пути

`rlm_start` не берёт никакой лок по пути проекта и не проверяет, есть ли уже другая сессия на том же каталоге. Несколько сессий на одном и том же пути — обычный сценарий (панель планирования + панель кодирования, несколько subagent'ов на одном fleet-юните) и он безопасен БЕЗ какой-либо координации между сессиями, потому что каждая просто открывает СВОЙ, независимый `IndexReader(db_path)` в режиме `PRAGMA journal_mode=WAL`: WAL-режим SQLite для того и существует, чтобы обслуживать произвольное число одновременных читателей без взаимной блокировки. Сессии друг про друга ничего не знают — и им незачем: на уровне БД координация уже есть.

Чтение (`rlm_start`, любой read-хелпер через `IndexReader`) и запись (`rlm_index build`/`update`) — асимметричны:

- **Чтение** ничем не сериализуется и не должно — это и есть смысл WAL.
- **Запись** сериализуется эксклюзивным OS-локом (`_BuildLock` в `bsl_index.py`): рядом с `bsl_index.db` кладётся файл `bsl_index.db.lock`, на него берётся неблокирующий `fcntl.flock` (Unix) / `msvcrt.locking` (Windows). Если ВТОРОЙ процесс (вторая сессия, второй `rlm-bsl-index` CLI-вызов) попытается запустить `build`/`update` на ТОМ ЖЕ пути, пока лок уже держит первый, — он не ждёт и не молча накладывает изменения поверх, а немедленно получает `RuntimeError: Index build already in progress`. Лок реентерабелен в рамках одного потока одного процесса (последовательные build→update внутри одного вызова), но чужому потоку/процессу того же db_path — отказ. Лок ОС-уровня: если процесс-держатель падает, ОС снимает лок сама, оставшийся lock-файл не мешает следующему успешному `acquire()`.
- **Чтение во время чужой записи** — открытый ДО начала `update` `IndexReader` не падает и не отдаёт битые данные, если другая сессия в этот момент пересобирает индекс in-place (DROP→CREATE в транзакции). Декоратор `_transient_safe` перехватывает ИМЕННО два известных транзитных класса `sqlite3.OperationalError` возможных в этом узком окне — `"no such table"` (застали между DROP и CREATE) и `"database is locked"` (короткая эксклюзивная блокировка best-effort `VACUUM`) — и деградирует их в свежий пустой/нулевой сентинел вместо падения. Любая ДРУГАЯ `OperationalError` (опечатка в SQL, `"no such column"`, `"disk I/O error"`, повреждение) пробрасывается как есть — этим достигается разница между «старый читатель пережил гонку с чужим rebuild» и «в коде реальный баг», которую by-design нельзя тихо проглотить. Практический эффект: старая сессия может увидеть на миг пустой результат вместо актуального, но не упадёт и не получит мусор; повторный вызов хелпера после завершения чужого `update` увидит уже свежие данные (открывать новую сессию не нужно).

Итого: конкурентные `rlm_start` на одном пути безопасны по чтению без каких-либо изменений в rlm-tools-bsl — координация уже есть на уровне SQLite (WAL) и на уровне файлового лока (запись). Специального session-level лока по пути в `server.py`/`session.py` нет и не планируется: он был бы избыточен поверх уже существующей защиты и без необходимости сериализовал бы независимые read-only сессии.

### Границы охвата и провенанса (v1.34.0)

Релиз read-time: схема SQLite, `BUILDER_VERSION` и путь билдера не тронуты (две
общие с ним функции обхода метаданных получили необязательный приватный
status-канал, но без него исполняется ПРЕЖНЯЯ ветка — тот же цикл по тем же
кандидатам; равенство выхода сверено с до-релизной реализацией), пересборка не
требуется. Меняется то, В КАКОЙ ФОРМЕ ответ доезжает до агента.

**Роль текущего root проходит через процессную границу, а не вычисляется заново.**
`rlm_start` уже выполнил `detect_extension_context`, поэтому
`current_config_role` / `current_config_name` / `current_config_root` /
`extension_name_by_root` прокладываются JSON-safe примитивами через
`_create_session_backend` → `ProcessBackendConfig`/init-payload → worker `Sandbox` →
`make_bsl_helpers`. Роль `EXTENSION` относится ТОЛЬКО к путям внутри фактического
`current_config_root`, а не автоматически ко всему `base_path` — это существенно для
поддержанного wrapper-входа, где `resolve_config_root` оставляет container, а CFE
лежит в его дочернем каталоге. Прямой (legacy) конструктор без этих аргументов
выполняет обещанный ленивый detector РОВНО один раз.

**Топология корней проверяется рано и один раз.** `validate_root_topology(base,
extension_paths)` требует, чтобы base и каждый extension-root были попарно
непересекающимися деревьями (ни равенства, ни containment в любую сторону). Ручной
overlapping/nested root в direct/embedding API получает ранний controlled
`ValueError`, а не двойную индексацию с ложными totals. Вызов идёт на входе
`make_bsl_helpers`, а на более широких границах (`Sandbox.__init__`, server, worker)
— ТОЛЬКО при активном BSL-пути (`format_info is not None and enable_bsl_helpers`); в
worker — ДО открытия его собственного `IndexReader`, чтобы ошибочный root не оставил
частично построенный backend. Документированный generic-режим provenance не
использует и валидации не получает.

**Detector-alias фильтруется до BSL transport.** Directory symlink/junction рядом с
базой может указывать ВНУТРЬ current root; такой кандидат — не соседний source-root,
а второй путь к уже учтённому дереву. Он убирается из `extension_paths`/name-map
ПЕРЕД созданием backend, поэтому штатный `rlm_start` не превращается в новый
topology-отказ. Публичный return `detect_extension_context`, index-builder и
override-consumers не меняются. На runtime тот же declared-owner-boundary применяется
при обходе CFE: до спуска проверяется КАЖДЫЙ directory symlink/junction
(`st_reparse_tag` на Windows — там `is_symlink()` для junction ложно), а не только
конечный файл, потому что обычный `.bsl` за junction симлинком не является.

**Каталог-редирект НИКОГДА не маршрут спуска.** Правило одно на обе цели, и обе
половины существенны: цель ВНЕ ext_root — чужое дерево (иначе main-модуль получил бы
ext-owner), цель ВНУТРИ ext_root — то же физическое поддерево, которое этот же
`os.walk` уже обходит по СВОЕМУ пути. `os.walk` отсекает POSIX-симлинк сам, но
junction для него обычный каталог, поэтому без этого правила каждый `.bsl` под
junction-ом возвращался дважды. Дедупликация по множеству уже посещённых целей
вторую половину не закрывает: junction верхнего уровня встречается РАНЬШЕ своей
глубоко лежащей цели и «выиграл» бы у неё — выдача зависела бы от порядка обхода.
Отказ от спуска делает победителем обычный физический путь всегда. Симметрично на
уровне файлов: один физический `.bsl` даёт ОДНУ строку, сколькими бы маршрутами он
ни доехал (симлинк на файл внутри того же root резолвится в ту же цель и без гарда
дал бы две строки с одинаковым `rel`).

**Untrusted путь и trusted строки каталога разведены.** Единственный untrusted вход
owner-алгоритма — прямой аргумент `get_module_outline`. Публичная оболочка
БЕЗУСЛОВНО (в частности, НЕ гейтясь на наличие расширений) прогоняет уже выбранный
locator через `_ext_resolve_safe` ровно один раз, поэтому путь вне песочницы и вне
корней расширений теперь даёт controlled access error вместо структурного ответа.
Раньше он доезжал до ответа: при `no_live=True` — до skipped-маркера вовсе без
обращения к ФС, при `no_live=False` `PermissionError` глотался и превращался в
`parse_failed`. Внутренние обёртки зовут непубличное trusted-ядро и второго
path-resolve не платят — fast-path списков не дорожает.

**Live-каталог отвязан от общего `_ensure_index()`.** BSL-only фаза
(`_ensure_extension_bsl_catalog`) даёт nearby-модули БЕЗ материализации main-таблицы,
без обхода metadata XML и без разбора synonyms, поэтому declaration-budget ограничивает
именно parse, а не «работает после того, как вся работа уже сделана». Main-часть идёт
через route-keyed кеш: канон `glob` (порядок и состав прежнего
`glob_files("**/*.bsl")` — он НАБЛЮДАЕМ, потому что решает, какие именно строки
отдадут `find_module`/`find_by_type`) и канон `walk` (состав прежнего прямого обхода
для indexed live-каталога). Каноны провабельно различны по порядку И составу, поэтому
не сливаются; каждый строится не более одного раза за сессию.

**Доказуемость поколения индекса.** Успешный SELECT не является сертификатом.
`IndexReader.get_build_capabilities()` одним чтением отдаёт build-опции, `bsl_count`,
фактический `COUNT(modules)`, `base_path`, `built_at`, `build_in_progress` и
`PRAGMA data_version`; public query-set обрамляется ДВУМЯ такими чтениями.
Транзиентное состояние (rebuild, смена поколения, sentinel) означает, что недоказуемы
САМИ СТРОКИ → маршрут уходит в status-aware live. Туда же ведёт НЕСОВПАДЕНИЕ
идентичности: сверяется не только стабильность `base_path` между двумя чтениями, но и
равенство его текущему корню сессии (нормализованно — регистр, завершающий разделитель
и `..` здоровый индекс в live не отправляют). Индекс от другого корня описывает чужой
домен: его строки не «неполны», они не относятся к делу, и `incomplete` (нижняя оценка
СВОЕГО домена) для них неверен по смыслу. На штатном серверном маршруте состояние
недостижимо — файл БД выбирается по md5 нормализованного `base_path`
(`get_index_db_path`), — но достижимо при legacy/direct embedding и подменённой БД.
Пустой `base_path` идентичность не опровергает и не судится; у боевого ридера это
состояние недостижимо (`get_build_capabilities` отдаёт `None`), и предпосылка
закреплена тестом. Стабильная неполнота домена
(`modules_count != bsl_count`) — другое: это доказанная неполнота НАБОРА, поэтому
пооперационные ветки сохраняют свой index-seek и публикуют нижнюю оценку, а разовая
загрузка каталога честно падает на glob/cache и ФАКТИЧЕСКИ возвращает пропущенный
файл. Для optional synonym/metadata-домена даже стабильного поколения недостаточно:
production-коллекторы v15 штатно пропускают unreadable/malformed XML, поэтому такой
домен консервативно помечается `index_coverage="build_unproven"` и `complete` в v15 не
производится вовсе. Строки этого домена гейтятся ровно на ПРИНАДЛЕЖНОСТИ базе, а не на
`index_coverage == "unavailable"`: у «unavailable» три причины (недоступные
capabilities, идущая пересборка, смена поколения между снимками), и в двух ПОСЛЕДНИХ
строки заведомо описывают правильную базу — они недоказуемы, но не ложны, а живого
сканера синонимов основной конфигурации в проекте нет, поэтому отказ от них означал бы
пустой `search_objects` на каждой пересборке. Первая причина о принадлежности не говорит
ничего, поэтому идентичность спрашивается ОТДЕЛЬНЫМ узким чтением
`IndexReader.get_declared_base_path()` (одна строка `index_meta`), а не берётся из
capability-снимка: `get_build_capabilities()` отдаёт `None` на любой негодности переписи
(нечитаемый `built_at`, отсутствующий `bsl_count`), и гейт по снимку молча пропускал бы
чужой индекс ровно у подменённой БД — там, ради чего сверка и заведена. Узкий метод
пробуется ПЕРВЫМ, а если ридер его не знает (`idx_reader` — duck-typed параметр, и
сторонний адаптер может быть собран под прежний API: неизвестный метод либо падает
`AttributeError`, либо молча отдаёт `None`), база берётся из capability-снимка —
compatibility-фолбэк, без которого такой адаптер потерял бы уже имевшийся у него гейт.
Чужая база —
единственная причина, при которой строки НЕВЕРНЫ; тогда счёт и список синонимов гейтятся
ОДНИМ условием (иначе развалился бы их задокументированный паритет), а перепись ссылок
проваливается в live-обход. Отсутствие заявления (`base_path` пуст либо ключа нет)
идентичность не опровергает и не судится.

## Мост предупреждений воркера (v1.34.0)

В дефолтном process-режиме у воркера нет своих обработчиков логирования, и до
`server.log` не доходило НИЧЕГО — половина арг-гардов диагностировалась только чтением
кода. Теперь на логгер `rlm_tools_bsl` внутри воркера навешивается ограниченная
очередь WARNING+ (DEBUG намеренно не мостится — обычная сессия раздула бы IPC и лог),
которая едет additive-ключом `log_records` в УЖЕ существующих `init_ok` и
`execute_result`: новый тип фрейма и версия протокола не заводятся.

Границы объявлены честно:

- **Родитель перепроверяет ВСЁ заново**, независимо от лимитов воркера: тип,
  количество (≤20), длину каждой записи (≤300) и печатность КАЖДОГО code point.
  Лимиты в воркере — гигиена, а не граница доверия: worker и есть та сторона, от
  компрометации которой защищает изоляция, иначе он получил бы возможность подделывать
  строки `server.log` (переводы строк, ESC, bidi). Нарушение — `SandboxProtocolError`.
- **Запись — только параметризованным вызовом логгера** и только после успешной
  валидации всего payload; каждая строка несёт `session_id`. В ответ агенту строки НЕ
  копируются: это диагностика оператора.
- **Startup-записи read-once**: их читает start-boundary сразу после создания backend,
  а если конструктор бросил ПОСЛЕ принятого `init_ok` (параллельный shutdown) — их
  забирает failure-callback, который затем ОБЯЗАТЕЛЬНО передаёт backend в reaper.
  Execute-boundary делает тот же drain в `finally` вокруг КАЖДОЙ попытки
  `backend.execute`, потому что lazy restart мог принять новый `init_ok`, а
  параллельный `rlm_end` — отозвать backend до `_finish_rlm_execute`.
- **Чего мост НЕ обещает.** Runtime-предупреждения, испущенные после последнего
  успешно принятого фрейма (hard timeout, `worker_error`, protocol error, потеря
  воркера), недостижимы: у мёртвого воркера очередь недоступна, а стримить её
  отдельным фреймом — уже смена протокола. Не обещан и хвост, отброшенный
  frame-fitting-ом при near-cap payload. Inline-режим мост НЕ использует вовсе —
  там логгер и так родительский, и двойной записи быть не должно.

## Честная граница безопасности

AST-гейт и урезанные builtins песочницы — это БАЗОВАЯ защита (defense-in-depth), а не
полноценная security-граница: она снижает СЛУЧАЙНЫЙ вред и закрывает дешёвые/известные
способы побега (запись файлов, сеть, опасные импорты, классический escape-гаджет через
dunder/интроспекцию, vars()/globals(), operator.attrgetter, license/credits/copyright,
мутация атрибутов объектов obj.attr=…).

Процессная изоляция v1.29.0 добавляет application-level containment (отдельные
namespace/память, hard-kill, resource limits), но **не является OS security boundary**:
worker работает с правами пользователя сервиса, и после успешного Python-escape ему
потенциально доступны файлы, сеть и environment (включая LLM-credentials), разрешённые
этому пользователю. Worker и parent под одним OS identity не образуют adversarial jail.
Для сильной защиты применяйте OS-jail средствами оператора: отдельная учётная запись,
read-only ACL на исходники, firewall/container policy — см. раздел «OS hardening» в
[INSTALL.md](INSTALL.md).

**Двухуровневый TTL сессий:** idle-сессии (без единого `execute`-вызова, `calls=0`) автоматически закрываются через 10 минут (`RLM_SESSION_TIMEOUT_IDLE`). Активные сессии (`calls>0`) живут до 30 минут (`RLM_SESSION_TIMEOUT_ACTIVE`). Единая переменная `RLM_SESSION_TIMEOUT` переопределяет оба значения (backward compat). Это защищает от «осиротевших» сессий, которые модель открыла, но не использовала.

## Честная обрезка списков

Архитектурная разница с типизированными MCP-серверами (см. «Совместное использование с
RAG» ниже) означает, что здесь нет единого генерического response-budget guard'а: `rlm_execute`
возвращает агенту голый `stdout`, сервер не видит структуру Python-объекта, который
хелпер построил внутри песочницы, и не может обрезать его «снаружи» уже после вызова.
Каждый хелпер, у которого список МОЖЕТ вырасти пропорционально размеру конфигурации,
обязан резать себя сам и честно об этом сообщать — единого перехватчика на уровне
транспорта, как в code-index (`cap_response`/`shrink_search_results`/`omit_oversize_sections`
в его `mcp/cap.rs`), в этой архитектуре быть не может.

Списочные поля хелперов делятся на три категории:

1. **Ограничены бюджетом самой сущности.** Запрос к индексу сам несёт `LIMIT`
   (`get_object_attributes(limit=2000)`, `get_predefined_items(limit=2000)`,
   `search_methods(limit=30)` и т.п.) — это НЕ решение уровня ответа, а параметр
   SQL-запроса; типичный вызов такого хелпера в разумной конфигурации не подходит
   к границе, но потолок стоит на случай патологии.
2. **Ограничены природой домена.** `find_scheduled_jobs`, `find_http_services`,
   `find_web_services`, `find_xdto_packages` — количество регламентных заданий или
   HTTP-сервисов в конфигурации фиксировано архитектурой самой конфигурации (единицы —
   десятки), а не пропорционально её размеру; explicit-cap здесь был бы решением
   несуществующей проблемы и лишним параметром в сигнатуре, который агент обязан был
   бы учитывать без всякой пользы.
3. **Пропорциональны размеру конфигурации и раньше НЕ были ограничены.** Список растёт
   вместе с конфигурацией (сколько объектов ссылается на этот enum, сколько документов
   пишет в этот регистр) — именно здесь исторически возникали необрезанные хвосты.
   Найдены и исправлены в этом релизе: `get_object_full_structure`'s
   `enum_values_for_typed_refs` (объект с несколькими enum-типизированными полями —
   каждое раскрытое перечисление раньше отдавалось ЦЕЛИКОМ) и `find_register_writers`
   (популярный регистр на большой ERP может иметь сотни статических писателей).

Для категории 3 (и для новых списочных хелперов вообще) конвенция — тот же словарь
полей, что уже устоялся в кодовой базе для похожих случаев (`find_functional_options`,
`find_call_hierarchy.truncated_targets`, `_RoleGroupBuilder.details_truncated`,
`posting_calls_offset` в `find_register_movements`): истинный `total`/`total_*` (не
меняется задним числом — обратная совместимость с кодом, который уже читает это поле),
`returned` (сколько реально показано), `has_more` (нужно ли просить продолжение), и
опционально `*_truncated` — словарь ПО ОБЪЕКТУ там, где обрезаемых сущностей может быть
несколько независимых (как в `enum_values_truncated`), а не единственное плоское число.
Дефолтный потолок выбирается ГЕНЕРОЗНЫМ (обычные конфигурации почти никогда не видят
`has_more=True`) — это защита от патологии, а не рычаг ежедневной пагинации; для
честного продолжения агенту достаточно передать явный больший `limit=`/`details_limit=`
у того же вызова, отдельного continuation-хелпера заводить не нужно.

## Хелперы песочницы

Полный список хелперов с описанием — см. **[HELPERS.md](HELPERS.md)**

55 BSL-специфичных хелпера + 8 стандартных I/O + 2 LLM = **65 хелперов**: навигация по модулям, парсинг процедур, граф вызовов (вкл. достижимость `find_path` и путь по метаданным `find_data_path`, v1.19.0), XML-метаданные, парсинг форм (обработчики/команды/атрибуты), поиск по бизнес-именам, области кода, заголовки модулей, анализ документооборота, **обзор объекта за 1 вызов через `get_object_profile` (v1.23.0, compact roll-up структуры/модулей/регистров/подписок/ролей/функц.опций)**, **обратный поиск ссылок на объект метаданных через `find_references_to_object` (v1.9.0, аналог конфигуратора «Найти ссылки → В свойствах»)** и **обратный поиск использований объекта В КОДЕ через `find_code_usages` (v1.14.0)**, **полнотекстовый поиск по всем файлам через `git_search` (v1.15.0, движок `git grep`, opt-in при исходниках под git)**, раскрытие ОпределяемогоТипа через `find_defined_types`, расширения (CFE) и многое другое.

### Оптимизации для совместимости с разными AI-моделями

Подробное описание оптимизаций — см. [HELPERS.md](HELPERS.md#оптимизации-для-совместимости-с-разными-ai-моделями)

## Пример работы агента: граф вызовов регистра ГрафикПлатежей

Реальный пример на конфигурации 1С:ERP (23 000+ файлов). Задача: найти все экспортные методы регистра сведений `ГрафикПлатежей` и построить граф вызовов.

```
Пользователь: "Найди все экспортные методы регистра сведений ГрафикПлатежей
               и построй по каждому граф вызовов"

Агент → rlm_start(path="D:/project/erp/code", query="...", effort="high")
# Сессия открыта, индекс 23K+ файлов загружен из кэша

Агент → rlm_execute (вызов 1):
    modules = find_module("ГрафикПлатежей")
    reg = [m for m in modules if m['category'] == 'InformationRegisters']
    exports = find_exports(reg[0]['path'])
    # Вывод: 11 экспортных методов (ManagerModule)

Агент → rlm_execute (вызов 2):
    for proc in export_names:
        L1 = find_callers_context(proc)          # кто вызывает
        for c1 in L1['callers']:
            L2 = find_callers_context(c1['caller_name'], module_hint=c1['object_name'])
            for c2 in L2['callers']:
                L3 = find_callers_context(c2['caller_name'], module_hint=c2['object_name'])
                print(f"→ {c1} → {c2} → {c3}")
    # Вывод: полный граф 3 уровня для 8 из 11 методов
    # Таймаут на инфраструктурных методах (ПроведениеДокументов → 150+ документов)

Агент → rlm_execute (вызов 3):
    # Дослать оставшиеся 3 метода — кэши прогреты, мгновенно
    # Вывод: граф для оставшихся методов
```

Результат (фрагмент):
```
УстановитьБлокировкиДанныхДляРасчетаГрафика
  → AccumulationRegisters.ЭтапыПроизводства.ПередЗаписью             [конечная]
  → AccumulationRegisters.РасчетыСПоставщиками.ПередЗаписью          [конечная]
  → AccumulationRegisters.РасчетыСКлиентами.ПередЗаписью             [конечная]
  → AccumulationRegisters.РасчетыПоФинансовымИнструментам.ПередЗаписью [конечная]
  → AccumulationRegisters.ДенежныеСредстваКВыплате.ПередЗаписью      [конечная]

РассчитатьГрафикПлатежейПоФинансовымИнструментам
  → InformationRegisters.ГрафикТраншейКредитовИДепозитов.ПриЗаписи   [конечная]
  → InformationRegisters.ГрафикОплатУслугПоАренде.ПриЗаписи          [конечная]
  → InformationRegisters.ГрафикОплатКредитовИДепозитов.ПриЗаписи     [конечная]
  → Catalogs.ВариантыГрафиковКредитовИДепозитов.ПриЗаписи            [конечная]
  → AccumulationRegisters.РасчетыПоФинансовымИнструментам.ПриЗаписи  [конечная]

РассчитатьГрафикПлатежейПоРасчетамСКлиентами
  → CommonModules.ОперативныеВзаиморасчетыСервер.ВыполнитьОтложенноеРаспределение
    → ExchangePlans.Полный.ПослеЗагрузкиДанных → ...ПередЗаписью
  → AccumulationRegisters.РасчетыСКлиентами.ПриЗаписи                [конечная]
```

**Итого: 3 вызова `rlm_execute`, 0 вызовов LLM.** Файлы целиком не читались — в контекст попали только print-результаты. Кэширование `extract_procedures` и результатов поиска позволило обработать 11 методов с тремя уровнями глубины на конфигурации из 23 000+ файлов.

## Совместное использование с RAG

В rlm-tools-bsl не встроены вызовы внешних RAG/MCP-серверов — и это осознанное решение. Инструмент остаётся автономным и не зависит от внешней инфраструктуры.

При этом AI-агент (Claude Code, Cursor, Windsurf) **уже является оркестратором**: он видит все подключённые MCP-серверы и сам комбинирует их вызовы в рамках одной задачи. Встраивать один инструмент в другой — значит делать за агента то, что он и так умеет.

Пример комбинированного workflow:
```
Агент видит MCP-серверы:
  - rlm-tools-bsl  (анализ исходников на лету, граф вызовов)
  - 1c-metacode    (индексированные метаданные, семантический поиск)

Агент получает задачу: "Как работает механизм оплаты?"

Шаг 1: 1c-metacode → search_metadata("Оплат") → список объектов
Шаг 2: rlm-tools-bsl → find_module("ОплатаПоставщику") → модули, код
Шаг 3: rlm-tools-bsl → find_callers_context("РассчитатьГрафикОплаты") → граф вызовов
        (NB: у ОбработкаПроведения вызов от платформы в граф не попадает, поэтому пустые
         callers — норма, а не мёртвый код; явный BSL-вызов обработчика, если он есть,
         хелпер всё же покажет)
Шаг 4: 1c-metacode → search_code("РассчитатьГрафик") → дополнительный контекст из индекса
```

Оба инструмента работают параллельно, каждый в своей зоне: rlm-tools-bsl — анализ кода на лету без подготовки, RAG — семантический поиск и структурные связи из индекса.

### Графовый мост (опционально): слияние на сервере вместо оркестрации агентом

Оркестрация агентом (выше) имеет цену: промежуточные ответы каждого MCP-сервера проходят через контекст агента. Для сценариев «rlm → граф → rlm» есть опциональный мост к 1c-mcp-metacode — по тому же паттерну, что `llm_query`: задан `RLM_METACODE_URL` → в песочнице появляются хелперы `graph_call`, `graph_tools`, `graph_search_code`, `graph_search_routines`, `graph_object_structure` (см. [HELPERS.md](HELPERS.md#graph-хелперы-мост-к-1c-mcp-metacode), [ENV_REFERENCE.md](ENV_REFERENCE.md#графовый-мост-1c-mcp-metacode)).

Выгода: скрипт в `rlm_execute` вызывает граф прямо из песочницы, сливает индексированный графовый контекст с on-the-fly RLM-данными на стороне сервера и печатает только итоговую выжимку — промежуточные graph-ответы в контекст агента не попадают.

Автономность сохранена: без `RLM_METACODE_URL` хелперы отсутствуют, зависимость от внешней инфраструктуры не появляется (мост использует MCP-клиент из уже имеющейся зависимости `mcp`).
